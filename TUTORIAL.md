# 《200 OK》玩法简介

你是一名网络管理员。不断有新用户接入网络，你要放服务器、拉线路，让每个数据包都能送达目的地。

**目标**：让每一份请求都得到 200 OK。数据包送不到会推高左下角的红色「总体负载」，涨到 100% 网络崩溃，本局结束。本局成功传输的数据总量（Mb）就是你的分数。

## 怎么玩

1. **放服务器**：点「新建服务器」（30 G）放到合适的位置。服务器是数据包的中转站，能处理数据、接线路。
2. **接用户**：用户会自动接入（也可以点「新用户」手动放）。新用户有 10 秒接入时间，之后才会收发数据。
3. **连线**：按住一个节点拖到另一个节点上松手，就建立一条连接。用户之间不能直连，必须经服务器中转；不能自连、不能重复连、线路不能交叉，每条连接都占用服务器的连接数。
4. **传数据**：用户之间会互相发送数据包，服务器负责中转。传输成功就赚 2～3 G。
5. **升级**：点击服务器，用金币提升「数据吞吐量」或「最大连接数」。用户越来越多，连接数往往先不够用，记得提前补服务器或升级。

一旦有数据包无路可走，负载就会上升。**断连 → 不可达 → 负载上涨 → 全网崩溃**，一条断掉的线最终会拖垮整张网。所以随时留意消息终端和负载条，及时补线路。

## 操作

- 点工具栏按钮放置节点（右键取消）
- 按住节点拖出预览线建立连接
- 右键点连线删除；右键可取消拖线
- 空白处拖动平移，滚轮缩放
- 空格暂停，Esc 退出