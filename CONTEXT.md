# CUC260905 项目领域

这是一个网络节点游戏原型，玩法包含节点搭建、数据包传输和指针交互。本文只定义项目中需要保持一致的领域词汇和边界；实现路径、调用顺序、Unity 接线和测试要求记录在 `AGENT.md` 及相应模块文档中。

## 网络领域

**节点**：网络拓扑中的一个参与者，必须拥有在当前架构实例内唯一的节点标识，并具有用户或服务器角色。
_避免_：对象、设备、实体（除非讨论 Unity 对象本身）

**用户节点**：数据包传输的起点或终点，代表网络中的用户侧参与者。
_避免_：客户端、玩家节点（除非语境明确是玩家操作）

**服务器节点**：可以承载数据包处理、并拥有服务器能力档案的节点。服务器身份本身不等于已经具备某项具体能力。
_避免_：主机、中心节点

**网络拓扑**：当前已注册节点及其网络边的整体关系。
_避免_：网络图、场景物体列表

**网络边**：拓扑中两个不同节点之间的无向连接关系；同一对节点只有一条关系，不因端点顺序不同而产生两条边。
_避免_：有向边、连线对象

**连线**：玩家或工具发起的一次建立网络边的操作过程；操作成功后，结果是网络拓扑中的网络边。
_避免_：拖拽本身（拖拽是输入手势，连线是领域操作）

**路径**：从一个节点到另一个节点、由连续网络边组成的可传输路线。
_避免_：线路、轨道

**数据包**：一次沿路径传输、会占用路径上服务器处理能力并可能产生传输结果的网络负载单位。
_避免_：消息（系统提示消息不是网络数据包）

**总传输量**：当前会话累计成功传输数据包的字节量统计，以 Mb 计；只累加传输成功的数据包大小（PacketSize），失败与不可达不统计。
_避免_：带宽、吞吐量（这些是服务器处理能力与负载视角）

**服务器能力**：服务器节点可用于处理数据包和承载网络边的能力档案，至少区分数据吞吐量与最大连接数两个维度。
_避免_：服务器等级（等级只是升级轨道的数值，不是能力本身）

**服务器负载**：服务器在最近时间窗口内已经承担的数据处理量，用于判断候选路径是否超出处理能力。
_避免_：网络压力、全局负载

**全局负载**：反映整体网络运行压力的全局状态；它与某一台服务器的处理负载不是同一个概念。
_避免_：服务器负载、带宽

**连接裁决**：对一次连线请求给出成功或具体失败原因的判断，依据包括节点角色、重复连接、节点位置、边交叉和服务器连接容量。
_避免_：连接验证（验证可作为过程描述，裁决是结果语义）

**升级轨道**：服务器能力的一条独立成长维度；当前项目区分数据吞吐量轨道和最大连接数轨道。
_避免_：等级、升级属性（除非明确指某一轨道）

**金币**：当前项目的单一货币余额，用于支付具有费用的服务器升级，并通过成功完成数据包传输获得奖励收入。
_避免_：资金、积分、资源

**系统消息**：面向某个消息终端发布的文本记录，属于提示和反馈，不等同于网络中传输的数据包。
_避免_：数据包、日志

**消息终端**：按目标标识接收并展示系统消息、同时保留有限历史的界面端点。
_避免_：聊天框、全局日志

## 指针交互领域

**指针信号**：设备在某一时刻产生的原始状态变化，描述按键、阶段、屏幕位置和时间，但不表达点击、拖拽或悬浮语义。
_避免_：点击事件、输入事件（除非泛指所有输入）

**指针帧**：同一处理帧收集到的指针信号快照，以及该帧最近的屏幕位置。它是多个交互功能共享的原始输入事实。
_避免_：点击帧、交互结果

**命中**：一次指针位置经过目标解析后得到的逻辑交互对象及其世界射线、命中点等上下文。
_避免_：Collider、GameObject（它们是 Unity 表现层细节）

**交互目标**：可以被指针解析到、并作为意图归属对象的逻辑对象根节点。
_避免_：按钮、组件、碰撞体

**交互能力**：交互目标对某一种意图提供的行为能力。当前基础能力包括点击、拖拽和悬浮；一个目标可以同时拥有多种能力。
_避免_：交互类型、输入组件

**意图**：输入经过目标和会话规则解释后形成的、面向交互能力的语义请求。
_避免_：原始输入、事件（事件是传播机制，意图是交互语义）

**点击意图**：同一交互目标按下并释放后形成的一次离散交互请求。
_避免_：按下、鼠标点击（后者可指设备信号）

**拖拽意图**：由持续位移形成的交互生命周期，包含开始、更新、结束或取消；拖拽开始后目标由按下时捕获。
_避免_：移动事件、平移（相机平移是具体业务）

**悬浮意图**：指针进入或离开交互目标时形成的状态变化请求，与是否按下按键无关。
_避免_：鼠标经过、Hover 状态（可作为代码标识）

**指针会话**：由某个指针和按键组合维护的按下到释放/取消期间的交互状态；它是点击和拖拽判定的最小状态单元。
_避免_：全局输入状态、手势状态

**输入门控**：在某个独占操作期间，禁止其他领域解释同一批世界输入的边界。当前放置模式会阻塞世界交互，但仍允许原始指针帧被消费。
_避免_：输入锁死、禁用输入（门控只限制指定语义）

## 放置与浏览领域

**放置模式**：用户选定可实例化对象后，等待把它放入世界的短生命周期操作状态。当前流程一次成功放置后退出，也可由右键取消。
_避免_：建造模式、编辑模式

**放置预览**：放置模式期间跟随指针显示的非正式视觉副本。它不是已放置实例，也不参与世界交互或物理拾取。
_避免_：正式对象、幽灵实体（“幽灵”只适合作为实现俗称）

**相机浏览**：用户通过空白区域平移、滚轮缩放或程序化聚焦改变观察范围的行为。
_避免_：相机移动（过于宽泛，可能包含表现层跟随）

**相机焦点**：相机当前观察的世界位置，不等同于 Camera Transform 的完整位置。
_避免_：相机位置、目标点（除非明确是一次聚焦请求）

**缩放**：相机观察范围的大小；在本项目中数值语义遵循 Unity 相机类型，数值越小表示观察更近。
_避免_：放大倍率（倍率可以用于配置，不能替代当前缩放值）

## 表现反馈领域

**反馈圆**：在世界背景层显示一段时间后消失的圆形视觉反馈，由位置、半径、颜色和持续时间描述。
_避免_：特效、命中特效（除非该反馈确实表达命中）
