# 《200 OK》概念设计

## 核心循环

```
放置服务器 ──▶ 拖拽连线（用户→服务器→用户）──▶ 数据包沿路径传输
    ▲                                            │
    └──── 用金币升级服务器（吞吐 / 连接数）◀──────┘（成功传输 → 赚金币）
```

**过程**：放置服务器节点、拖线连接用户与服务器；用户之间经服务器中转互相发送数据包，传输成功赚取金币，金币用于新建/升级服务器，维持整张网运转。

**限制**：连线不能交叉；用户不能直连，必须经服务器中转；每条连接都占用服务器的连接数上限。

**压力**：用户不断接入，数据包随机发送；数据包无法送达会推高全局负载，负载到顶全网崩溃，本局结束。

**结算**：本局累计成功传输的数据总量（Mb）即为分数。